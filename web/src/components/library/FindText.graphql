query FindText($author: String!, $book: String, $title: String!) {
  findText(author: $author, book: $book, title: $title) {
    id
    title
    lines {
      id
      line
      lineNumber
      lineLabel
    }
    book {
      id
      title
    }
    author {
      id
      name
    }
  }
}
