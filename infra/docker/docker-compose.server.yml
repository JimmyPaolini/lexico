version: "3"
services:
  server:
    image: jimmypaolini/lexico-server
    build:
      context: .
      dockerfile: ./server.Dockerfile
    container_name: server
    ports:
      - 3001:3001
    env_file: ../../server/.env
    depends_on:
      - database